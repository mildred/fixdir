#! /bin/zsh
# similar to `.zlogin', except that it is sourced before `.zshrc'. It is meant
# as an alternative to `.zlogin' for ksh fans; the two are not intended to be
# used together, although this could certainly be done if desired. `.zlogin'

# Because this file is sourced just after /etc/profile, we have to regenerate
# the local profile

##### ENF OF FILE #####

